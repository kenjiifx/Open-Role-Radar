name: Sync Fast Path

# Same method Simplify uses under the hood: poll public Greenhouse / Lever / Ashby
# board JSON APIs frequently, plus their GitHub dump as a discovery signal.
# Publishes live-feed without waiting on slow Workday boards.

on:
  schedule:
    # GitHub Actions practical floor ~5 minutes — beat Simplify's ~30m GitHub dump.
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: openroleradar-sync-fast
  cancel-in-progress: true

jobs:
  sync-fast:
    name: Fast ATS + Simplify discovery
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Install uv
        uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
        with:
          enable-cache: true
          cache-dependency-glob: engine/uv.lock

      - name: Sync Python dependencies
        working-directory: engine
        run: uv sync --all-extras --dev

      - name: Restore live state
        uses: actions/cache@d4323d4df104b026a6aa633fdb11d772146be0bf # v4.2.2
        with:
          path: .local/state
          key: live-state-${{ github.run_id }}
          restore-keys: |
            live-state-

      - name: Sync fast adapters in parallel
        working-directory: engine
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Public ATS APIs (Simplify's real method) + their listings dump for discovery.
          OPENROLERADAR_ADAPTERS: greenhouse,lever,ashby,simplify
        run: uv run openroleradar sync --adapters greenhouse,lever,ashby,simplify

      - name: Build data exports
        working-directory: engine
        run: uv run openroleradar build-data

      - name: Publish live-feed branch
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          if [ ! -d site/public/data ]; then
            echo "site/public/data missing — build-data did not export a feed" >&2
            exit 1
          fi
          WORK="$(mktemp -d)"
          cp -R site/public/data/. "$WORK/"
          cd "$WORK"
          git init
          git checkout -b live-feed
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          printf '%s\n' '# Live job feed — force-pushed every fast sync' > README.md
          git add -A
          git commit -m "live-feed $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          git remote add origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push -f origin live-feed

      - name: Publish durable live-state release
        continue-on-error: true
        working-directory: engine
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: uv run openroleradar publish-state

      - name: Save live state
        uses: actions/cache/save@d4323d4df104b026a6aa633fdb11d772146be0bf # v4.2.2
        with:
          path: .local/state
          key: live-state-${{ github.run_id }}
